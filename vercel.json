{
  "buildCommand": "pnpm --filter @workspace/pwa run build",
  "outputDirectory": "artifacts/pwa/dist/public",
  "installCommand": "pnpm install --frozen-lockfile",
  "framework": null,
  "rewrites": [
    { "source": "/(.*)", "destination": "/index.html" }
  ]
}
